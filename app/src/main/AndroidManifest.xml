<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ao.newtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true">
        <activity android:name=".login"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".XO_App.Main2Activity"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".XO_App.XOX_Class"
            android:label="@string/xoxo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".course.Full_stack_course"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".course.Ios_course"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".course.Android_course"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".splash_screen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>